import {ConstantsCollection} from '../../../SimulationModule';
import {BUILDING_TYPE} from './BUILDING_TYPES';
import {BUILDINGS_ACTIONS} from './BUILDING_ACTIONS';

export const getConstants = (): ConstantsCollection => ({
  BUILDING_TYPE,
  BUILDINGS_ACTIONS
});
