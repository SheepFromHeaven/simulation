import {ConstantsCollection} from '../../../SimulationModule';
import {RESOURCE_TYPE} from './RESOURCE_TYPES';
import {RESOURCE_ACTIONS} from './RESOURCE_ACTIONS';

export const getConstants = (): ConstantsCollection => ({
  RESOURCE_TYPE,
  RESOURCE_ACTIONS
});
